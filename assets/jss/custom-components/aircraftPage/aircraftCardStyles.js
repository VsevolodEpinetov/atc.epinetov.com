import typography from "../typography"

const aircraftCardStyles = {
  ...typography,
  margins: {
    margin: '0 auto 3em'
  },
  thumbnail: {
    display: 'block',
    cursor: 'pointer'
  }
};

export default aircraftCardStyles;
